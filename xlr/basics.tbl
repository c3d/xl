// Types must come first for type info to be initialized correctly
NAME(nil);
NAME(self);
NAME(true);
NAME(false);

TYPE(boolean);
TYPE(integer);
TYPE(real);
TYPE(text);
TYPE(character);
TYPE(tree);
TYPE(symbol);
TYPE(name);
TYPE(operator);
TYPE(infix);
TYPE(prefix);
TYPE(postfix);
TYPE(block);

TYPE(integer8);
TYPE(integer16);
TYPE(integer32);
TYPE(integer64);
TYPE(unsigned);
TYPE(unsigned8);
TYPE(unsigned16);
TYPE(unsigned32);
TYPE(unsigned64);
TYPE(real32);
TYPE(real64);

