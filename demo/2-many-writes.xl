// Send many requests to the remote target.
// Note that this sends a local variable (defined here) over to target
I is 0
while I < 100 loop
    write_line "Sent request ", I
    tell "localhost",
        write_line "Hello world from PID ", process_id, " request #", I
    I := I + 1
