// Test that the macro system sort of works
// RUN=cat %c

xl.replace (DEFAULT) with (1.0)

xl.replace XLDeclarations(let 'X') with ('X' : real := DEFAULT)

let K
let L
let O

xl.replace ('K' + 'K') with ('K' * 2)

S : integer
T : integer := (S+S) + (S+S)
