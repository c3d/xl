// Test that the macro system sort of works
use XL.UI.CONSOLE

xl.replace (DEFAULT) with (4)

xl.replace XLDeclarations(let 'X') with ('X' : integer := DEFAULT)

let K
let L
let O

xl.replace ('K' + 'K') with ('K' * 2)

S : integer := 17
T : integer := (S+S) + (S+S)

WriteLn "K=", K
WriteLn "L=", K
WriteLn "O=", K
WriteLn "S=", S
WriteLn "T=", T
