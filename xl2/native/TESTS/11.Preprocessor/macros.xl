// Test that the macro system sort of works
use XL.UI.CONSOLE

transform (DEFAULT) into (4)

transform XLDeclarations(let 'X') into ('X' : integer := DEFAULT)

let K
let L
let O

transform ('K' + 'K') into ('K' * 2)

S : integer := 17
T : integer := (S+S) + (S+S)

WriteLn "K=", K
WriteLn "L=", K
WriteLn "O=", K
WriteLn "S=", S
WriteLn "T=", T
