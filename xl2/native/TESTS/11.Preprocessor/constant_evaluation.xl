// Check the behavior of constant_evaluation
use XL.UI.CONSOLE

xl.replace (fact 0) with (1)
xl.replace (fact 'X') with (xl.value('X' * xl.value(fact ('X'-1))))

K : integer := fact(5)

WriteLn "K=", K
