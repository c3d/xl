// CMD=%x -nobuiltins -parse %f -style html -show
// This is showing some comments

if x < 3 then // These comments are after then
   if y < 3 then // So are these comments
      z := 3 // After zed
      foo // After foo
      bar /* After bar */

      // Before zoo
      zoo

// What about these comments here?
boooo
