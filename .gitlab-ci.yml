before_script:
  - git submodule update --init --recursive

test:
  script:
  - make PREFIX=/tmp/xl/
  - make PREFIX=/tmp/xl/ install
  - export PATH=$PATH:/tmp/xl/bin
  - make test

make_only:
  script:
  - make
  - make opt
  - make release
  - make debug
